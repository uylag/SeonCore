#pragma once

namespace seoncore::policy
{

    /** @brief Policy tag: fixed dense storage. */
    struct fixed_dense   {};
    /** @brief Policy tag: fixed sparse storage. */
    struct fixed_sparse  {};

};
